{
  "buildCommand": "npm --prefix curalink-frontend install && npm --prefix curalink-frontend run build",
  "outputDirectory": "curalink-frontend/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
